/- LEAN4 FILE AUTO GENERATED BY CQOTL PROVER FOR OBLIGATION: obligation_4 -/
/- Proof by Iván Renison -/

import QHilbert.Commons
import QHilbert.LinearMapPropositions
import QHilbert.ProjectionSubmodule

variable {𝕜 : Type*} [_inst : (RCLike 𝕜)]

local notation "𝕜²" => ((EuclideanSpace 𝕜) (Fin 2))

noncomputable def vplus : 𝕜² := ketP

noncomputable def H : 𝕜² →ₗ[𝕜] 𝕜² := Hadamard

lemma obligation_4 :
    ((LinearMap.instLoewnerPartialOrder.le ketbra0) ((H.adjoint * ((((outerProduct 𝕜) vplus) vplus) : 𝕜² →ₗ[𝕜] 𝕜²)) * H)) := by
  rw [H, vplus, ← ketbraP, adj_Hadamard_ketbraP_eq', ketbra0]
